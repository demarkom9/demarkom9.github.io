# ShowCount.py
"""Shows how to use a for-loop for counting."""

# Assign to N the number of integers between and including 1 and 100000
# that are divisible by 2, 3, and 7.

N = 0

for k in range(1, 100001):
    if k % 2 == 0 and k % 3 == 0 and k % 7 == 0:
        N = N + 1

print(N)
